# Copie para .env.local e preencha — nunca commitar credenciais reais.
# Toda credencial (banco, APIs) deve vir de variável de ambiente, nunca
# hard-coded no código-fonte.

# DATABASE_URL=
# GEMINI_API_KEY=

# Sessão mock (ver src/lib/session.ts) — registro do atleta "logado" em dev,
# até existir autenticação real.
NEXT_PUBLIC_MOCK_ATLETA_REGISTRO=376380
